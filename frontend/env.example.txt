## PocketBase API 地址
## 本地开发：
# VITE_PB_URL=http://127.0.0.1:8090
##
## 线上部署（强烈建议用 https 反代，避免 Mixed Content）：
# VITE_PB_URL=https://your-domain.com/pb

## 二期真推送（默认关闭，只有已配置 Firebase/google-services.json 时才开启）
# VITE_ENABLE_PUSH_REGISTRATION=0





# AI 集成 (SiliconFlow / DeepSeek)
SILICONFLOW_API_KEY=sk-xxxxxxxxxxxxxx

# 后端管理脚本用 (scripts/*.mjs)
PB_ADMIN_EMAIL=admin@example.com
PB_ADMIN_PASSWORD=your-secure-password
