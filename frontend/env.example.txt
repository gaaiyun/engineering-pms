# PocketBase API 地址
# 本地开发
VITE_PB_URL=http://127.0.0.1:8090

# 生产构建建议使用 HTTPS 同源反向代理，例如：
# VITE_PB_URL=https://example.com/pb

# AI Provider、Base URL、模型和 API Key 由管理员在“系统管理 -> AI 配置”中保存到服务端。
# 禁止把 LLM API Key 写入 Vite 环境变量、浏览器 localStorage 或跟踪文件。

# 原生推送注册；未配置对应推送服务时保持关闭。
VITE_ENABLE_PUSH_REGISTRATION=0
