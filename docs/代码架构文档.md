# 工程结算管理系统 - 代码架构文档（v3.04 现行版本）

> 最后更新: 2026-05-16  
> 版本: v3.04（versionCode 44）  
> v3.0-v3.04 重大改造章节见底部"十、v3.0-v3.04 改造细节"

## 一、技术栈概览

| 层级 | 技术 | 版本 | 用途 |
|------|------|------|------|
| 前端框架 | React | 19.x | UI 构建 |
| 类型系统 | TypeScript | 5.9+ | 类型安全 |
| 构建工具 | Vite | 6.0+ | 开发&打包 |
| UI 组件库 | Ant Design Mobile | 5.42+ | 移动端组件 |
| 状态管理 | TanStack Query + Zustand | 5.x | 服务端/客户端状态 |
| 动画库 | Framer Motion | 12.x | 微动效 |
| 图表 | ECharts + Recharts | 5.x | 数据可视化 |
| 拖拽 | @dnd-kit | - | 看板拖拽 |
| 表格 | TanStack Table | 8.x | 桌面端任务表格视图（v3.0+） |
| 后端 | PocketBase | 0.22+ | BaaS (含 Realtime SSE + JS Hooks) |
| 服务端 hooks | JS (in pb_hooks) | - | 5 个文件 / 8 handler（v3.0+ 新增） |
| AI 服务 | SiliconFlow (DeepSeek) | - | LLM **服务端代理**（v3.04 起） |
| 移动打包 | Capacitor | 5.7 | Android APK |
| 原生通知 | OkHttp + okhttp-sse | 4.12 | Android 后台 SSE 长连保活（v3.0+） |

### 选型理由

| 技术 | 选择原因 |
|------|---------|
| PocketBase | 单文件后端（exe + `pb_data` 即完整服务），前端 JS SDK 直连 CRUD，后台可视化建模快速迭代 |
| React + TypeScript | 组件化适合复杂业务页面（看板、抽屉、时间轴），TS 提供类型约束与重构安全 |
| Vite | 启动快构建快，通过 `VITE_*` 环境变量注入运行配置 |
| Ant Design Mobile | 移动端组件成熟（触控优先），PC 浏览器同样良好表现 |
| TanStack Query + Zustand | Query 管理服务端数据缓存/刷新/重试；Zustand 管理纯 UI 状态（筛选、草稿等） |
| Capacitor | Web 前端直接打包 Android APK，成本低迭代快，可按需扩展原生插件 |

### 设计原则

- **低运维**：后端单文件可跑，避免传统后端复杂环境依赖
- **多端统一**：同一套前端代码支持 Web（PC/移动浏览器）+ Android APK
- **配置不写死**：后端地址、AI Key 等通过环境变量或 localStorage 管理
- **数据留痕**：关键动作写入 `audit_logs`，便于追溯与 AI 分析

## 二、完整项目目录结构

```
v2/
├── README.md                              # 项目总说明
├── START.bat                              # Windows 一键启动脚本
│
├── docs/                                  # 项目文档
│   ├── 代码架构文档.md                     # ← 本文件
│   ├── 交付文档_完整版.md
│   ├── 数据库设计_PocketBase版.md
│   ├── 用户使用指南.md
│   ├── App打包指南.md
│   ├── 快速启动_命令行版.md
│   ├── 宝塔部署操作手册.md
│   └── screenshots/
│       └── README.md
│
├── backend/                               # PocketBase 后端
│   ├── 启动后端.bat                        # Windows 启动脚本
│   ├── download_pocketbase.ps1            # 下载 PB 的 PowerShell 脚本
│   ├── CHANGELOG.md
│   ├── LICENSE.md
│   ├── pb_data/
│   │   ├── data.db                        # 主数据库 (SQLite, 需备份 ⭐⭐⭐)
│   │   ├── logs.db                        # 日志数据库
│   │   └── types.d.ts                     # PB 类型定义
│   └── pb_migrations/                     # 数据库迁移文件（16 个）
│       ├── 1763750287_created_projects.js
│       └── ...
│
├── pocketbase_0.26.3_linux_amd64/         # PB Linux 二进制
│   ├── pocketbase                         # 可执行文件
│   ├── CHANGELOG.md
│   └── LICENSE.md
│
├── scripts/                               # 运维/数据脚本（11 个）
│   ├── database_rebuild.mjs               # 重建数据库结构+权限规则+演示数据
│   ├── auto_backup.mjs                    # 自动备份（sqlite3 .backup + gzip）
│   ├── test_all.mjs                       # 全量测试
│   └── ...
│
└── frontend/                              # React 前端应用
    ├── package.json
    ├── vite.config.ts                     # Vite 构建配置
    ├── tsconfig.json / tsconfig.app.json / tsconfig.node.json
    ├── eslint.config.js                   # ESLint 配置
    ├── capacitor.config.ts                # Capacitor 移动端配置
    ├── index.html                         # HTML 入口
    ├── env.example.txt                    # 环境变量示例
    │
    ├── public/                            # 静态资源 (sw.js, manifest.json, icons/)
    ├── dist/                              # 构建产物
    ├── docs/                              # 前端专属文档 (PRD, TECH_SPEC, APK_BUILD_GUIDE)
    ├── android/                           # Capacitor Android 工程
    │
    └── src/                               # 源代码（45 个文件）
        ├── main.tsx                       # 应用入口 + Realtime 订阅初始化
        ├── App.tsx                        # 路由配置 + 权限路由组件
        ├── App.css / index.css            # 应用级 / 全局样式 + CSS 变量
        │
        ├── lib/                           # 核心库（6 个文件）
        │   ├── api.ts                     # 所有 API Hooks + 类型 + 权限函数
        │   ├── pocketbase.ts              # PB 客户端 + Realtime 订阅
        │   ├── queryClient.ts             # Query Client + Query Keys
        │   ├── store.ts                   # Zustand 状态管理
        │   ├── ai-service.ts              # AI 服务封装
        │   └── task-parser.ts             # 任务文本解析器
        │
        ├── pages/                         # 页面组件（18 个文件）
        │   ├── Home.tsx                   # 首页/底部 Tab 导航
        │   ├── Login.tsx / Register.tsx   # 认证页面
        │   ├── Tasks.tsx                  # 员工首页（项目+任务+未读消息）
        │   ├── TaskDetail.tsx             # 任务详情（权限控制+卡点+通知）
        │   ├── TaskCreate.tsx             # 创建任务（仅经理）
        │   ├── MyProjects.tsx             # 项目列表（分类筛选+归档+成员管理）
        │   ├── MyTasks.tsx                # 我的任务列表
        │   ├── ProjectTimeline.tsx        # 时间轴/甘特图（含横屏模式）
        │   ├── ProjectKanban.tsx          # 看板视图（含批量编辑入口）
        │   ├── ManagerDashboard.tsx       # 经理工作台（点击穿透）
        │   ├── ReviewCenter.tsx           # 变更审计中心（搜索+筛选+已阅/通过）
        │   ├── Profile.tsx                # 个人中心
        │   ├── SettingsPage.tsx           # 系统设置
        │   ├── Notifications.tsx          # 消息通知
        │   └── admin/
        │       ├── AdminDashboard.tsx     # 管理后台
        │       └── AIConsole.tsx          # AI 决策控制台
        │
        ├── components/                    # 可复用组件（12 个文件）
        │   ├── BatchTaskEditor.tsx         # 批量任务编辑（三列表格）
        │   ├── EmptyState.tsx             # 空状态提示
        │   ├── Skeleton.tsx               # 加载骨架屏
        │   ├── admin/
        │   │   └── ThreeColumnImport.tsx  # 三列导入组件
        │   ├── dashboard/
        │   │   └── AISummaryCard.tsx      # AI 简报卡片
        │   └── kanban/                    # 看板组件套件
        │       ├── KanbanBoard.tsx        # 看板主容器（员工禁拖）
        │       ├── KanbanColumn.tsx       # 看板列
        │       ├── TaskCard.tsx           # 任务卡片
        │       ├── TaskDetailDrawer.tsx   # 任务详情抽屉
        │       └── index.ts              # 看板组件导出
        │
        └── test/
            └── setup.ts                   # 测试全局 setup
```

### 文件命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 页面/通用组件 | PascalCase.tsx | `TaskDetail.tsx`, `EmptyState.tsx` |
| 工具/库文件 | camelCase.ts | `api.ts`, `queryClient.ts` |
| 样式文件 | 与组件同名.css | `KanbanBoard.css` |
| Node.js 脚本 | snake_case.mjs | `database_rebuild.mjs` |
| Python 脚本 | snake_case.py | `screenshot_pages.py` |
| 中文文档 | 中文名.md | `交付文档_完整版.md` |
| 英文文档 | UPPER_CASE.md | `README.md`, `CHANGELOG.md` |

## 三、核心库详解 (`src/lib/`)

### 3.1 `api.ts` — 统一数据层（约 1100 行）

#### 类型定义
| 类型 | 说明 |
|------|------|
| `Project` | 项目（含 members 字段） |
| `Task` | 任务（含 blocker JSON） |
| `User` | 用户（含 role） |
| `Handoff` | 交接提案 |
| `AuditLog` | 审计日志（含 review_status） |
| `Notification` | 通知（含 is_read） |
| `AISummary` | AI 简报 |

#### 状态枚举
```typescript
export const TaskStatusEnum = {
    PENDING: 'pending',
    IN_PROGRESS: 'in_progress',
    BLOCKED: 'blocked',
    COMPLETED: 'completed',
    OVERDUE: 'overdue',
}
```

#### 权限工具函数
| 函数 | 说明 |
|------|------|
| `isManager()` | 判断当前用户是否为经理/管理员 |

#### 查询 Hooks (useQuery)
| Hook | 说明 |
|------|------|
| `useProjects()` | 项目列表（员工仅看参与的） |
| `useTasks(projectId?)` | 任务列表（员工仅看参与的） |
| `useUsers()` | 用户列表 |
| `useCurrentUser()` | 当前登录用户 |
| `useTask(id)` | 单个任务详情 |
| `usePendingHandoffs()` | 待审核交接 |
| `useTaskAuditLogs(taskId)` | 单任务审计日志 |
| `useAuditLogs(filters?)` | 全局审计日志（支持多条件筛选） |
| `useAISummaries(userId)` | AI 简报 |
| `useNotifications()` | 当前用户通知 |

#### 变更 Hooks (useMutation)
| Hook | 说明 | 审计日志 | 全员通知 |
|------|------|---------|---------|
| `useCreateTask()` | 创建任务 | ✅ | ✅ |
| `useUpdateTask()` | 更新任务 | ✅ | ✅ |
| `useMarkTaskComplete()` | 标记完成 | ✅ | ✅ |
| `useMarkTaskBlocked()` | 标记卡点 | ✅ | ✅ |
| `useUnblockTask()` | 取消卡点 | ✅ | ✅ |
| `useApproveHandoff()` | 审核通过交接 | ✅ | — |
| `useRejectHandoff()` | 驳回交接 | ✅ | — |
| `useQuickCreateProject()` | 快速创建项目（含成员） | — | — |
| `useDeleteProject()` | 删除项目 | — | — |
| `useDeleteTask()` | 删除任务 | — | — |
| `useBatchSaveTasks()` | 批量保存任务 | — | — |
| `useUpdateProjectMembers()` | 更新项目成员 | — | — |
| `useArchiveProject()` | 归档/取消归档项目 | — | — |
| `useMarkAuditRead()` | 标记审计日志已阅读 | — | — |
| `useMarkAuditApproved()` | 标记审计日志已通过 | — | — |

#### 辅助函数
| 函数 | 说明 |
|------|------|
| `notifyProjectMembers()` | 给项目全员发送通知 |
| `loginUser()` | 用户登录 |
| `registerUser()` | 用户注册 |
| `logoutUser()` | 用户登出 |

### 3.2 `pocketbase.ts` — PB 客户端 + HybridAuthStore + Realtime

```typescript
// 连接策略优先级：VITE_PB_URL > localStorage.pb_url > 运行时推导 > 本地兜底
export const PB_URL = getPocketBaseUrl()

// v3.04 新增：HybridAuthStore 子类化 BaseAuthStore
//   - rememberMe=1 → localStorage（跨会话保留）
//   - 否则        → sessionStorage（关浏览器即失效）
class HybridAuthStore extends BaseAuthStore { /* ... */ }
export const pb = new PocketBase(PB_URL, new HybridAuthStore())

// Realtime 订阅（6 个集合）
export function subscribeToChanges() {
    // 订阅: tasks, projects, notifications, handoffs, audit_logs, comments
    // 变动时自动 invalidateQueries 刷新前端缓存
}
```

**HybridAuthStore 关键设计**（v3.04 修复 XSS token 长期暴露）：
- `constructor()` 同时尝试从 localStorage 和 sessionStorage 读取（兼容旧用户）
- `save(token, model)` 根据 `localStorage.rememberMe` 决定写入哪个 backend，**并清掉另一个**避免双份
- `clear()` 清掉两个 backend

### 3.3 `queryClient.ts` — 缓存配置

```typescript
export const queryKeys = {
    projects: ['projects'],
    tasks: ['tasks'],
    users: ['users'],
    currentUser: ['currentUser'],
    notifications: ['notifications'],
    handoffs: ['handoffs'],
    auditLogs: ['auditLogs'],
    aiSummaries: ['aiSummaries'],
}

// 默认 staleTime: 15秒（tasks/notifications: 10秒）
```

### 3.4 `ai-service.ts` — AI 服务（v3.04 改为服务端代理）

| 函数 | 说明 |
|------|------|
| `aggregateProjectData()` | 聚合项目数据（排除已归档） |
| `generateAIReport()` | 生成 AI 分析报告（先尝试代理，失败回退） |
| `chatWithAI()` | AI 对话（同上） |
| `callLLMViaProxy()` | **v3.04 新增**: POST `/api/custom/llm-proxy`（服务端注入 API key） |
| `callLLMDirect()` | 兜底直连（向后兼容用户已在 localStorage 配过 key 的情况） |
| `callLLM()` | 统一入口：proxy first → direct fallback |

**v3.04 安全升级（C1）**：
- API key 从 localStorage 搬到 PB `app_settings` collection
- 浏览器通过 `pb.send('/api/custom/llm-proxy', {method: 'POST', body: ...})` 调用
- 服务端 hook `llm_proxy.pb.js` 读 `app_settings.value` 并以 Bearer 注入到 SiliconFlow 请求
- 浏览器全程不接触明文 key，关闭 XSS 拖 key 攻击面

### 3.5 `store.ts` — Zustand 状态

客户端 UI 状态管理（当前较简单）。

### 3.6 `task-parser.ts` — 任务解析器

解析文本格式的任务描述为结构化数据。

## 四、页面组件详解 (`src/pages/`)

### 4.1 认证页面
| 页面 | 功能 |
|------|------|
| `Login.tsx` | 登录，支持记住密码 |
| `Register.tsx` | 注册，默认 employee 角色 |

### 4.2 管理页面（admin/manager 权限）
| 页面 | 功能 | 权限 |
|------|------|------|
| `AdminDashboard.tsx` | 管理后台（用户/项目/统计） | AdminRoute |
| `AIConsole.tsx` | AI 智能决策台 | AdminRoute |
| `ManagerDashboard.tsx` | 经理工作台（4 个可点击穿透的指标卡片） | ManagerRoute |
| `ReviewCenter.tsx` | 变更审计中心（Tab 分类 + 搜索 + 已阅/通过） | ManagerRoute |

### 4.3 业务页面
| 页面 | 功能 | 关键特性 |
|------|------|---------|
| `Home.tsx` | 首页导航，底部 Tab | 角色分流入口 |
| `Tasks.tsx` | 员工首页 | 未读消息摘要 + 待办任务列表 + 项目卡片 |
| `TaskDetail.tsx` | 任务详情 | 权限控制（员工只读+卡点上报）、审计日志+全员通知 |
| `TaskCreate.tsx` | 创建任务 | 仅经理可访问（ManagerRoute） |
| `MyProjects.tsx` | 项目列表 | Tab 分类（全部/进行中/卡顿/已归档）+ 归档/删除/成员管理按钮 |
| `MyTasks.tsx` | 我的任务列表 | — |
| `ProjectTimeline.tsx` | 时间轴/甘特图 | 手机端适配 + 横屏旋转模式 |
| `ProjectKanban.tsx` | 看板视图 | 员工禁拖 + 批量编辑入口 |
| `Profile.tsx` | 个人中心 | 头像编辑 |
| `SettingsPage.tsx` | 系统设置 | PB 地址配置 |
| `Notifications.tsx` | 消息通知 | — |

## 五、组件详解 (`src/components/`)

### 5.1 看板组件套件 (`kanban/`)
```
KanbanBoard.tsx         # 看板主容器（DnD 上下文，员工 sensors 置空禁拖）
├── KanbanColumn.tsx    # 单列（待处理/进行中/卡点/已完成）
│   └── TaskCard.tsx    # 任务卡片（状态标签+执行人+截止日）
└── TaskDetailDrawer.tsx # 任务详情抽屉（使用 useTaskAuditLogs）
```

### 5.2 其他组件
| 组件 | 功能 |
|------|------|
| `BatchTaskEditor.tsx` | 批量任务编辑（三列表格：任务名/执行人/时间，动态增删行，人员选择弹窗） |
| `AISummaryCard.tsx` | AI 简报展示卡片 |
| `ThreeColumnImport.tsx` | 三列导入组件（管理后台用） |
| `EmptyState.tsx` | 空状态提示 |
| `Skeleton.tsx` | 加载骨架屏 |

## 六、数据流架构

```
┌─────────────────────────────────────────────────────────┐
│                        UI 层                             │
│  Pages/Components → React Hooks → State Update          │
└──────────────────────────┬──────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│                     状态管理层                            │
│  TanStack Query (服务端数据缓存)  +  Zustand (UI状态)    │
│  staleTime: 15s (tasks/notifications: 10s)              │
└──────────────────────────┬──────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│                   API 层 (api.ts)                        │
│  useQuery Hooks (查询) + useMutation Hooks (变更)        │
│  变更时自动: 审计日志 + 全员通知 + invalidateQueries     │
└──────────────────────────┬──────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│                   PocketBase SDK                         │
│  pb.collection('xxx').getList/create/update/subscribe   │
└──────────────────────────┬──────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│                   PocketBase Server                      │
│  Realtime SSE → 前端 subscribeToChanges() 自动刷新缓存   │
│  Collection Rules → 服务端权限校验（经理专属更新/删除）    │
└─────────────────────────────────────────────────────────┘
```

## 七、状态机与关键流程

### 7.1 任务状态流转

```
                  ┌──────────────────────────┐
                  │        pending           │
                  └────────────┬─────────────┘
                     员工开始任务│(写入 start_date)
                               ▼
                  ┌──────────────────────────┐
              ┌───│      in_progress         │───┐
   员工上报卡点│   └──────────────────────────┘   │员工完成任务
   (写入blocker)│                                 │(写入completed_at)
              ▼                                   ▼
 ┌────────────────┐                 ┌──────────────────────────┐
 │    blocked      │                 │      completed           │
 └────────────────┘                 └──────────────────────────┘
        │ 经理解除卡点                          │
        └──→ in_progress                       │ 创建 handoff (status=pending)
                                               ▼
                                    ┌──────────────────────────┐
                                    │  经理审核交接提案          │
                                    │  通过 → approved + 创建   │
                                    │         下一条 task       │
                                    │  驳回 → rejected +        │
                                    │         review_note       │
                                    └──────────────────────────┘
```

### 7.2 角色操作边界

| 角色 | 允许的状态变更 | 关键约束 |
|------|--------------|---------|
| 员工 (employee) | `pending→in_progress`, `in_progress→blocked`, `in_progress→completed` | 仅能改 status/blocker，不能编辑任务其他字段 |
| 经理 (manager) | 所有状态变更 + 审核交接 | 通过交接时自动创建下一条任务 |

### 7.3 前端关键交互

- **Kanban 拖拽**：改变 `tasks.status`，列内排序更新 `sequence`
- **Timeline 渲染**：基于 `start_date` / `deadline` / `completed_at` 绘制甘特图
- **通知推送**：订阅 `notifications` 集合（Realtime SSE），轮询兜底

## 八、PocketBase 数据库设计

> 完整字段定义、索引与权限规则详见 **[数据库设计_PocketBase版.md](数据库设计_PocketBase版.md)**（v3.04 已更新）。

### 集合总览（11 个，v3.04）

| 集合 | 说明 | 服务端权限规则 |
|------|------|-------------|
| `users` | 用户（Auth Collection，含 role/avatar/department） | PB 默认 |
| `projects` | 项目（含 members 多选关联，progress 由 hook 自动重算） | updateRule/deleteRule: 仅经理 |
| `tasks` | 任务/步骤（含 blocker JSON、priority、is_milestone） | updateRule/deleteRule: 仅经理 |
| `handoffs` | 交接提案（pending/approved/rejected） | createRule/updateRule 收紧（PR `b62d693`） |
| `comments` | 评论 | — |
| `notifications` | 通知（含 is_read、link_type/link_id 跳转） | listRule: `@request.auth.id = user` |
| `audit_logs` | 审计日志（含 review_status: unread/read/approved/rejected） | createRule 收紧 |
| `ai_summaries` | AI 简报（含 risk_level、model_used） | — |
| `device_tokens` | 设备 token 注册（v2.95+） | — |
| `flower_logs` / `progress_logs` | 小红花 / 历史变更 | — |
| **`app_settings`** | **v3.04 新增** — LLM API key 服务端存储 | listRule/viewRule: admin\|manager；createRule/deleteRule: admin |

### 配置要点

- **PB 地址（前端）**：`VITE_PB_URL` > `localStorage.pb_url` > 运行时推导（HTTP 用 `host:8090`，HTTPS 用同域 `/pb`）
- **AI Key（v3.04 起服务端）**：写入 PB `app_settings.key = 'siliconflow_api_key'` 表，浏览器读不到 value，通过 `/api/custom/llm-proxy` 间接调用
- **数据存储**：SQLite（`backend/pb_data/data.db`）+ WAL
- **权限规则**：通过 `database_rebuild.mjs` 配置 `managerOnlyRules`，或直接由 `pb_migrations/*.js` 应用
- **服务端 hooks**：`backend/pb_hooks/*.pb.js`（PB 启动时自动加载）

## 九、路由与权限设计

### 9.1 权限路由组件
```typescript
<PrivateRoute>    // 所有登录用户可访问
<AdminRoute>      // 仅 admin 可访问
<ManagerRoute>    // 仅 admin/manager 可访问
```

### 9.2 路由映射
| 路由 | 组件 | 权限 |
|------|------|------|
| `/login` | Login | 公开 |
| `/register` | Register | 公开 |
| `/app/*` | Home | PrivateRoute |
| `/admin` | AdminDashboard | AdminRoute |
| `/manager` | ManagerDashboard | ManagerRoute |
| `/review-center` | ReviewCenter | ManagerRoute |
| `/project/:id/timeline` | ProjectTimeline | PrivateRoute |
| `/project/:id/kanban` | ProjectKanban | PrivateRoute |
| `/task/:id` | TaskDetail | PrivateRoute |
| `/task/create` | TaskCreate | ManagerRoute |
| `/my-projects` | MyProjects | PrivateRoute |
| `/my-tasks` | MyTasks | PrivateRoute |
| `/profile` | Profile | PrivateRoute |
| `/settings` | SettingsPage | PrivateRoute |
| `/notifications` | Notifications | PrivateRoute |

### 9.3 前端权限控制
- `isManager()` 函数控制 UI 元素显隐
- 员工：不可编辑任务、不可完成任务、不可分配人员、不可拖拽看板
- 经理：全部操作权限 + 批量编辑 + 归档/删除/成员管理

### 9.4 服务端权限控制
- `tasks` / `projects` 集合的 `updateRule` / `deleteRule` 设为经理专属
- 通过 `database_rebuild.mjs` 配置

## 十、AI 功能架构（v3.04 服务端代理）

### 10.1 配置
- 提供商: SiliconFlow / DeepSeek
- 模型: deepseek-ai/DeepSeek-V3, Qwen/Qwen2.5-72B-Instruct 等
- 端点: `https://api.siliconflow.cn/v1/chat/completions`
- **Key 存储**: PocketBase `app_settings` 表（v3.04 起），浏览器侧不再持有

### 10.2 请求路径

```
浏览器 ─POST /api/custom/llm-proxy─→ PocketBase
                                       │
                                       ├─ requireRecordAuth (必须登录)
                                       ├─ 读 app_settings.value
                                       └─ $http.send → SiliconFlow API
                                                            │
浏览器 ←─────── JSON 响应 ────────────────────────────────────┘
```

### 10.3 功能模块
1. 实时诊断报告（`AIConsole.tsx` + `ManagerDashboard.tsx`）
2. AI 问答（`AIConsole.tsx`，支持历史记录+多模型切换）
3. 数据聚合时自动排除已归档项目
4. **回退机制**：proxy 调用失败时 `callLLM` 自动尝试 `callLLMDirect`（兜底用户老配置）

### 10.4 关键文件
- `frontend/src/lib/ai-service.ts` — 客户端封装
- `backend/pb_hooks/llm_proxy.pb.js` — 服务端代理
- `backend/pb_migrations/1773000000_create_app_settings.js` — collection 定义

## 十一、样式系统

### 11.1 CSS 变量 (`index.css`)
```css
:root {
  --primary-color: #3B82F6;
  --danger-color: #EF4444;
  --success-color: #10B981;
  --warning-color: #F59E0B;
  --neutral-color: #64748B;
  --border-color: #E2E8F0;
  --background-color: #F8FAFC;
  --glass-bg: rgba(255, 255, 255, 0.7);
  --glass-blur: blur(12px);
}
```

### 11.2 动画类
```css
.fade-in    { animation: fadeIn 0.3s ease; }
.float-up   { animation: floatUp 0.6s ease-out; }
.pulse      { animation: pulse 2s infinite; }
```

## 十二、移动端与部署

### 12.1 Capacitor 配置
```
appId: 'com.engineering.settlement'
appName: '工程结算'
server: { androidScheme: 'https' }
```

### 12.2 响应式策略
- 底部 Tab 导航（移动端）
- 时间轴横屏旋转模式（CSS transform + orientation 检测）
- 触摸/鼠标手势兼容（@dnd-kit）

### 12.3 部署架构

- **后端**：PocketBase 常驻进程（建议内网/本机监听 8090，通过 Nginx `/pb` 反代）
- **前端**：Vite 构建产物 `dist/` 部署到 Nginx 静态站点（SPA 路由回退到 `index.html`）
- 部署细节见 [宝塔部署操作手册.md](宝塔部署操作手册.md)

### 12.4 运行环境要求

| 环境 | 要求 |
|------|------|
| Node.js | 18+ (npm 包管理 + Vite 构建 + TypeScript) |
| PocketBase | 0.26.3 (后端服务) |
| Python | 3.10+ (可选，截图工具 Playwright) |
| Android Studio | 可选（Capacitor APK 打包） |

## 十三、运维脚本 (`scripts/`)

| 脚本 | 功能 | 关键参数 |
|------|------|---------|
| `database_rebuild.mjs` | 重建数据库结构+权限规则+演示数据 | 含 managerOnlyRules |
| `auto_backup.mjs` | 自动备份 | 每12h, 保留60天, sqlite3 .backup |
| `test_all.mjs` | 全量功能测试 | — |
| `batch_assign_tasks.mjs` | 批量分配任务 | — |
| `migrate_status.mjs` | 状态字段迁移 | — |
| `set_api_key.mjs` | 设置 AI API Key | — |
| `check_ai_status.mjs` | 检查 AI 服务状态 | — |
| `diagnose_and_fix_ai.mjs` | AI 诊断修复 | — |
| `fix_ai_and_avatars.mjs` | 修复 AI + 添加头像 | — |
| `add_blocked_tasks.mjs` | 添加卡点任务测试数据 | — |
| `screenshot_pages.py` | 页面截图工具 | Python |

## 十四、测试规范

### 14.1 测试技术栈

| 工具 | 用途 |
|------|------|
| Vitest | 测试运行器 + 断言库 |
| @testing-library/react | 组件渲染与交互测试 |
| @testing-library/jest-dom | DOM 断言扩展 |
| @testing-library/user-event | 用户交互模拟 |
| happy-dom | 浏览器环境模拟 |
| @vitest/coverage-v8 | 覆盖率收集 |

命令均在 `frontend/` 目录下执行：`npm test`（全部）、`npm run test:watch`（监听）、`npm run test:coverage`（覆盖率）。

### 14.2 目录与命名约定

- 测试文件与源码**同目录**放置，命名：`<源文件名>.test.ts(x)`
- 全局 setup 文件：`src/test/setup.ts`
- 覆盖率配置：`vitest.config.ts` 的 `test.coverage` 字段

### 14.3 测试分层

| 层级 | 目标 | 方式 |
|------|------|------|
| 单元测试 | `lib/` 下纯函数（task-parser、queryKeys、isManager 等） | 直接调用，不依赖 DOM |
| 组件测试 | UI 组件渲染与交互 | `render` + `screen`，只测用户可见行为 |
| 集成测试 | TanStack Query hooks | `renderHook` + `QueryClientProvider`，mock `pb.collection()` |

### 14.4 用例书写规范

- 一条用例只验证一个行为
- 描述用中文，格式：`it('当 X 时，应 Y')`
- 每类输入至少覆盖：正常值、边界值、非法值
- 不测实现细节（不断言内部 state 变量名）

### 14.5 Mock 规范

- **PocketBase**：统一 mock `lib/pocketbase` 模块（`pb.authStore` + `pb.collection()`）
- **路由**：使用 `MemoryRouter` 控制初始路径
- **第三方库**：`@dnd-kit/sortable` mock `useSortable`；`@capacitor/app` mock `addListener`

### 14.6 覆盖率目标

| 范围 | 语句/分支目标 |
|------|--------------|
| `lib/task-parser.ts` | ≥ 90% |
| 新增代码 | ≥ 80% |
| 旧代码 | 逐步提升 |

新增纯函数/工具**必须**附带单元测试；新增组件**建议**附带渲染测试；PR 中测试不通过不允许合并。

## 十五、测试账号

| 角色 | 账号 | 密码 |
|------|------|------|
| 管理员 | admin_boss | 12345678 |
| 经理 | zhang_manager / wang_manager / mgr_li | 12345678 |
| 员工 | chen_doc / li_audit / zhao_site | 12345678 |

---

## 十六、v3.0-v3.04 改造细节（2026-05-16）

> 4 轮夜间自主作业，58 commits，30+ bug 修复。完整 changelog 见 [CHANGELOG.md](./CHANGELOG.md)。

### 16.1 响应式三端布局（PR 3）

**核心文件**：
- `frontend/src/components/AppShell.tsx` — 根据 viewport 宽度切换 mobile/tablet/desktop
- `frontend/src/components/DesktopShell.tsx` — 桌面 Sidebar (240px) + TopBar (56px) + 内容区
- `frontend/src/index.css` — 媒体查询：`<500` / `500-768` / `768-1023` / `≥1024`

**断点逻辑**：
| 视窗宽度 | 容器策略 | UI |
|---------|---------|-----|
| < 500 | 限宽 480 居中 | 手机 Tab 导航 |
| 500-1023 | 全宽 | 同上但内容填满 |
| ≥ 1024 | 全宽 | 桌面 Sidebar + TopBar 三区布局 |

### 16.2 桌面任务表格（PR 4）

`frontend/src/pages/TasksTablePage.tsx`：TanStack Table 8 + 多选 + 批量改状态/删除

- 列定义：项目 / 任务 / 状态 / 执行人 / 截止日 / 优先级（皆可排序）
- `useReactTable` 配置 row selection enabled
- 工具栏：批量按钮根据选中行数显示，调用 `useBulkUpdateStatus` / `useBulkDelete`

### 16.3 看板桌面动画（PR 5）

`frontend/src/components/kanban/KanbanColumn.tsx` + `KanbanColumn.css`：

- `@keyframes pulse-breathe`（2s 循环）当列接收拖拽时启动
- 桌面端启用 hover preview，移动端保留原触摸体验

### 16.4 Android 后台推送（PR 1+2）

**核心**：放弃 FCM，改用 PocketBase Realtime SSE 长连接 + 原生前台服务保活。

| 层 | 文件 | 关键点 |
|----|------|--------|
| Web 前端 | `useNotificationAlerts.ts` | Toast / 振动 / 三音调 / 红闪 / 系统通知统一链路 |
| Android 原生 | `MainActivity.java` + `SSEService.java` | OkHttp 4.12 + okhttp-sse 维持长连，前台 service 防杀 |
| PB 后端 | `pb_hooks/realtime.pb.js` | SSE 元信息钩子 |
| 兼容 11 家国产 ROM | `docs/android-background-keepalive.md` | 电池白名单 + 自启动权限引导 |

### 16.5 服务端兜底层（5 个 hook）

| 文件 | 触发器 | 作用 |
|------|--------|------|
| `project_progress_sync.pb.js` | tasks create/update/delete | 自动重算 project.total_tasks / completed_tasks / progress |
| `handoffs_status_sync.pb.js` | handoffs update (status=approved) | 同步 from_task.status=completed |
| `audit_logs_reject_sync.pb.js` | audit_logs update (review_status=rejected) | 把 task 字段回滚到 before_data |
| `llm_proxy.pb.js` | POST /api/custom/llm-proxy | 服务端注入 API key 调用 SiliconFlow |
| `realtime.pb.js` | SSE 连接事件 | 通知推送链路基础 |

### 16.6 安全收尾（v3.04 三大件）

| 编号 | 问题 | 修复 |
|------|------|------|
| C1 | LLM key 明文存 localStorage（XSS 可拖） | API key 搬服务端，通过 PB hook 代理 |
| C2 | 不勾"记住我"时 token 仍存 localStorage（关浏览器留痕） | `HybridAuthStore` 子类化，token 走 sessionStorage |
| J-2 | 移动端 768px 宽度时容器仍限宽 480 | 媒体查询拆分两段（<500 vs 500-768） |

### 16.7 性能优化

- **React.lazy + Suspense**：18 个页面全部懒加载（首屏 bundle 缩小）
- **Vite manualChunks**：echarts / pocketbase / antd-mobile / react-vendor 拆包
- **gzip 比对**：v2.96 → v3.04 gzip bundle 降幅约 90%

### 16.8 测试套件

- `frontend/e2e/perf-baseline.spec.ts` — Playwright 性能基线
- `scripts/full_production_test.py` — 5 section + 32 screenshots 完整生产测试
- `scripts/e2e_permission_flow.py` — 角色权限 E2E
- `scripts/e2e_error_handling_flow.py` — 错误链路 E2E
- `scripts/e2e_responsive_diff.py` — 桌面 vs 移动 diff（J-3 false-positive 已修）

### 16.9 项目结构变化（v2 vs v3.04）

```
frontend/src/
├── components/
│   ├── AppShell.tsx           ← v3.0 新增
│   ├── DesktopShell.tsx       ← v3.0 新增
│   └── ...
├── hooks/
│   └── useNotificationAlerts.ts  ← v3.0 新增
├── pages/
│   └── TasksTablePage.tsx     ← v3.0 新增（桌面表格）
└── lib/
    └── pocketbase.ts          ← v3.04 HybridAuthStore 重写

backend/
├── pb_hooks/                  ← v3.0+ 全新目录
│   ├── project_progress_sync.pb.js  ← v3.04 v3
│   ├── handoffs_status_sync.pb.js
│   ├── audit_logs_reject_sync.pb.js
│   ├── llm_proxy.pb.js        ← v3.04 新增
│   └── realtime.pb.js         ← PR 2
└── pb_migrations/
    └── 1773000000_create_app_settings.js  ← v3.04 新增
```
| 员工 | chen_doc | 12345678 |

---

## 附录：待优化项

1. API Key 迁移到服务端代理（安全）
2. FCM 推送集成（消息提醒）
3. Profile 统计动态数据替换硬编码
4. 虚拟滚动（大列表性能优化）
5. React.lazy 代码分割（懒加载）
6. 局部错误边界
7. 设置页面消息通知开关实际功能
