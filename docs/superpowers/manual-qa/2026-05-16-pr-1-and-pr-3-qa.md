# 手动验证 Checklist — PR 1（通知一期收尾 v2.97）+ PR 3（响应式 AppShell）

**日期：** 2026-05-16
**触发条件：** 用户醒后做的真机/浏览器验收。所有自动化测试（npm test 125/125、tsc 0 错、build 成功、APK 编译通过）已绿。

---

## A. v2.97 APK 真机回归（PR 1）

**APK 位置：** `G:\项目管理软件_v2\EngineeringPMS_v2.97_notification_consolidated.apk`（6.2 MB）

### 安装与基础
- [ ] 装到至少一台真机（推荐你常用的那台）
- [ ] 启动 app，登录测试账号
- [ ] **登录后不立即响铃**（基线建立中，prev=N，不应认为 N 都是新消息）

### 前台通知主链路
让另一账号给测试账号建任务/发评论，触发 task_assigned 通知：
- [ ] 系统通知栏弹出 "工程结算管理 · 您有 N 条新消息"
- [ ] 振动两阵（[200,100,200,100,200] 模式）
- [ ] 三音调提示音（523Hz + 659Hz + 784Hz，类微信）
- [ ] App 内顶部红色 Toast "收到 N 条新消息"（6 秒后消失）
- [ ] 全屏红色边框闪烁 ~2.8 秒
- [ ] 通知 Tab 红点数与实际未读一致

### 边界场景
- [ ] 切换到另一账号 → 红点数立刻重置（不会沿用上一账号）
- [ ] 退出登录后再登录 → 历史未读不重新触发响铃
- [ ] 标记某条已读后 unread 减少 → **不响铃**（避免标记已读时还提示）

### 已知未解决（PR 2 才覆盖）
- ⚠️ App 在后台/锁屏下，5 分钟后让另一账号建任务 → **目前可能收不到**
  - 这是 PR 2 (Android 前台服务 + SSE) 要解决的，本 PR 不阻塞
  - 计划：用 PocketBase Realtime SSE + Android ForegroundService 保活长连

---

## B. 浏览器响应式 AppShell（PR 3）

**dev server 启动方式：**
```bash
cd "G:/项目管理软件_v2/frontend"
npm run dev
# 同时另一终端启动后端
cd "G:/项目管理软件_v2/backend"
./pocketbase.exe serve
```

### 桌面端（≥1024px）
- [ ] Chrome 1920×1080：左侧 240px Sidebar 显示
- [ ] Sidebar 项目：首页 · 我的任务 · 我的项目 · 审核中心* · 通知 · 设置 · 管理后台*
  - `*` 项只对 admin/manager 可见
- [ ] EngineeringPMS Logo 在 Sidebar 顶部
- [ ] 顶栏 56px：左侧搜索占位框 / 右侧通知铃（红点数）/ 用户名 + 头像
- [ ] 当前页面在 Sidebar 高亮（左侧紫色边）
- [ ] 主区背景浅灰 (#f8fafc)，业务页内容居中渲染

### 平板（769-1023px）
- [ ] Chrome 800×600：Sidebar 折叠为 64px 仅图标
- [ ] hover 图标显示 tooltip（NavLink 的 title 属性）
- [ ] Logo 文字隐藏，仅图标
- [ ] 顶栏不变
- [ ] 业务页正常显示

### 移动端（<768px）
- [ ] Chrome 375×667（设备模拟）：无 Sidebar、无顶栏
- [ ] Home.tsx 的底部 TabBar 仍正常显示
- [ ] APK 真机：完全保持原 v2.96 视觉（移动端不退化）

### 拖动窗口宽度切换
- [ ] 慢慢拖窗口从 1920 → 800 → 600：
  - 1024 节点：Sidebar 从 240 → 64 折叠（200ms 过渡动画）
  - 768 节点：Sidebar 整体消失，回到移动布局
- [ ] 反向拖回 → 平滑还原，无闪烁

### 通知铃 + 用户菜单
- [ ] 通知铃显示 unreadCount 红点徽章（数字 > 0 时显示，0 不显示）
- [ ] 点铃 → 跳 `/notifications`
- [ ] 点用户菜单（头像 + 用户名）→ 跳 `/settings`

### 路由切换
- [ ] 点 Sidebar 每一项都能正确跳转
- [ ] 在 Sidebar 切换路由时，左侧紫边正确移动
- [ ] 普通用户登录：Sidebar 没有"审核中心"和"管理后台"
- [ ] admin 登录：Sidebar 全部 7 项可见

### 通知 + AppShell 共存
- [ ] 登录后 dev server 给账号生成通知 → AppShell 内的 TopBar 通知铃红点更新
- [ ] 红闪 overlay 在 AppShell 容器外层全屏覆盖（z-index 99999 高于 Sidebar）
- [ ] Toast、振动、提示音照常工作

---

## C. 如果发现 bug 怎么办

1. 记录现象（截图 + 路径）
2. 如果阻塞 → 回滚最近一个 commit：`git revert HEAD`
3. 如果非阻塞 → 在 plan 里加 Task 并 fixing

---

## D. 通过后的下一步

PR 1 + PR 3 验证通过后，可继续：
- **PR 2**：Android 前台服务 + SSE 长连（解决后台/锁屏推送）
- **PR 4**：任务表格视图 + 批量操作
- **PR 5**：看板/甘特拖拽增强（用 SVAR React Gantt，研究报告推荐）
- **PR 6**：文档收尾

详见 `docs/superpowers/specs/2026-05-16-pms-notification-and-desktop-design.md`。
