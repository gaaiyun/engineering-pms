# 开发交接

更新时间：2026-07-13。

## 单一事实源

接手顺序：

1. `README.md`
2. `docs/开发交接.md`
3. `docs/需求实现对照表.md`
4. `docs/代码架构文档.md`
5. `docs/数据库设计_PocketBase版.md`
6. `docs/宝塔部署操作手册.md`

旧的原始会话、调试截图、夜间报告和危险数据库脚本不再是当前事实源。

## Git

- 功能分支：`codex/web-route-dataflow-fixes-20260711`
- GitHub PR：`gaaiyun/engineering-pms#1`
- 不直接推送 `main`，不 force push，不跳过 hooks。
- commit 使用中文 `类型(范围): 描述`，不得加入工具/模型署名或 `Co-Authored-By`。
- 代码、后端迁移、测试和文档清理分开提交。

## 版本

| 项目 | 当前值 |
|---|---|
| 应用 | 3.05 / Android versionCode 45 |
| PocketBase Server | 0.22.21 |
| PocketBase JS SDK | 0.21.5 |
| React | 19.2 |
| Capacitor | 5.7 |

PocketBase 暂不升级。现有 hooks/migrations 依赖 0.22 API，升级必须另开分支测试。

## 已完成实现

- 导航、角色路由和旧链接统一。
- 项目详情、看板、时间轴、通知、资料和系统页面连通。
- 手机整体密度下调，时间轴加入筛选和缩放。
- 注册、保持登录、管理员账号 CRUD、停用和删除保护。
- 用户/项目/任务最小权限与项目成员回填。
- 完成、卡点、解阻、交接审批和任务删除服务端事务。
- 通用 LLM Provider 配置与服务端代理。
- Android/PWA 正式图标与可复现 Debug APK 构建。
- 默认头像使用姓名末两个汉字的大字字标；真实上传照片优先。
- 看板区分卡片点按与独立拖拽手柄，并修复跨泳道拖动持续命中自身的问题。
- PocketBase `logs.db` 故障隔离、systemd 守护和专用账户运行。

## 本轮验证

- Vitest：25 个文件、186 项通过。
- Playwright：28 个跨角色、跨断点、项目/资料和看板拖拽场景通过。
- TypeScript + Vite production build 通过。
- 本轮变更源文件 ESLint 通过。
- 隔离 PocketBase：业务库迁移成功；项目越权不可见；停用 token refresh 401；卡点/解阻/完成/交接事务与并发审批通过；失败回退无脏状态；任务事务删除通过。

所有数字在新提交前必须用实际命令刷新，不能复制为永久结论。

## 尚未完成的外部状态

- 2026-07-13 已完成生产冷备、增量部署 `1783841200` 至 `1783841400` 和当前 hooks；PocketBase `enabled/active`，`data.db` 与 `logs.db` 的 `quick_check` 均为 `ok`，发布后日志未再出现损坏或启动错误。
- 生产账号已按私有清单同步为 1 个启用管理员和 5 个启用员工；旧测试账号已停用，账号清单和初始密码不得提交 Git。
- Android 16 真机已完成 3.05 主流程和实时通知验收；包含最终姓名字标头像与看板拖拽修复的 APK 已重建，但手机已断开，仍需下次连接后覆盖安装复验这两项。
- 生产 AI Provider 需要管理员保存配置后再做 200/choices 验收。
- Release APK 缺私有 keystore。
- 生产 HTTP Web 已原子切换到 3.05 静态资源；根路径、深链和静态资源均返回 200。HTTPS 到服务器 IP 的 TLS 握手仍失败，不能把 HTTP 可用等同于 HTTPS 可用。

## 2026-07-13 生产验收记录

- 后端按迁移历史差异增量发布，未整目录覆盖；第一次发现历史迁移同集合冲突后自动恢复冷备，最终增量发布成功。
- 结构迁移不得把既有 `is_active=false` 猜成“未回填”。三个历史停用账号在发布后按私有清单恢复为停用，当前启用账号仍为 1 个 admin 和 5 个 employee。
- Web 发布保留上一版目录，当前根页面、`/system/users` SPA 深链和构建资源均为 200，根页面与深链返回同一入口文件。
- 生产浏览器验收覆盖桌面 1440×900 与手机 390×844：工作台、项目列表、真实项目详情、看板、通知、用户管理和资料页通过；桌面仅侧栏、手机仅底栏，控制台和页面异常为空。
- 生产看板受控拖拽从“待开始”到“进行中”成功并恢复原状态；测试通知在另一个已打开页面中无刷新出现，随后删除，数据库复核无测试通知残留。
- 浏览器导航时中止旧页面 SSE/未读请求会记录 `net::ERR_ABORTED`，属于页面切换取消请求，不是服务错误。

最终 Debug APK：versionName `3.05`、versionCode `45`、`7,210,637` bytes、SHA-256 `7D78A618DCCD13498F3C3A6EAB279AE3823B5D4B8AECEFE460D15DD6FB8A6447`。

## 生产发布检查点

1. 每次发布仍需重新冷备 `pb_data`，确认业务库 `quick_check=ok`。
2. 先比对生产 `_migrations` 与仓库文件，只部署尚未应用且经过审查的迁移；历史分叉环境禁止整目录覆盖。
3. 跑账号、权限、事务、通知和 AI 负面/正面测试。
4. PocketBase 与 Web 已发布；下一步是保存生产 AI Provider 后验收调用，以及手机重新连接后覆盖安装最终 APK。
5. 真实员工账号按 username 幂等同步；岑锴源账号为 admin，其余角色由私有部署清单决定。
6. 检查 GitHub PR 文件树、敏感信息、提交消息和 CI/本地测试。

## 接力记录格式

每次接手只追加当前事实，不复制旧长篇会话：

```text
时间：
目标：
分支/提交：
修改文件：
验证命令与结果：
生产部署状态：
未完成/回滚点：
```
