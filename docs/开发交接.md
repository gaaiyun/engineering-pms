# 开发交接

更新时间：2026-08-20。

## 单一事实源

接手顺序：

1. `README.md`
2. `docs/开发交接.md`
3. `docs/需求实现对照表.md`
4. `docs/代码架构文档.md`
5. `docs/数据库设计_PocketBase版.md`
6. `docs/宝塔部署操作手册.md`
7. `docs/MCP服务交接.md`

旧的原始会话、调试截图、夜间报告和危险数据库脚本不再是当前事实源。

## Git

- 当前交付分支：`codex/delivery-20260820`
- 3.05 历史开发分支：`codex/web-route-dataflow-fixes-20260711`；对应 GitHub PR #1。
- 不直接推送 `main`，不 force push，不跳过 hooks。
- commit 使用中文 `类型(范围): 描述`，不得加入工具/模型署名或 `Co-Authored-By`。
- 代码、后端迁移、测试和文档清理分开提交。

## 版本

| 项目 | 当前值 |
|---|---|
| 应用 | 3.06 / Android versionCode 46 |
| PocketBase Server | 0.22.21 |
| PocketBase JS SDK | 0.21.5 |
| React | 19.2 |
| Capacitor | 5.7 |

PocketBase 暂不升级。现有 hooks/migrations 依赖 0.22 API，升级必须另开分支测试。

## 已完成实现

- 导航、角色路由和旧链接统一。
- 项目详情、看板、时间轴、通知、资料和系统页面连通。
- 手机整体密度下调，时间轴加入筛选和缩放。
- 注册、保持登录、管理员账号 CRUD、停用和删除保护。
- 用户/项目/任务最小权限与项目成员回填。
- 完成、卡点、解阻、交接审批和任务删除服务端事务。
- 通用 LLM Provider 配置与服务端代理。
- Android/PWA 正式图标与可复现 Debug APK 构建。
- 默认头像使用姓名末两个汉字的大字字标；真实上传照片优先。
- 看板区分卡片点按与独立拖拽手柄，并修复跨泳道拖动持续命中自身的问题。
- PocketBase `logs.db` 故障隔离、systemd 守护和专用账户运行。

## 3.06 最终验证

- 后端 QA：既有基线用例 `64/64`，最终差异用例 `15/15`。
- 前端测试：`203/203`；Playwright：`28/28`；MCP：`11/11`。
- TypeScript/Vite production build、Hook 语法检查和 APK 静态验收通过。
- 生产 Realtime SSE 请求返回 200。

## 2026-08-20 生产交付

- 生产部署使用提交 `c4c298f`，完成时间为 2026-08-20 06:45 CST。
- 发布前备份的精确路径记录于私有运维接力文档。
- Web 入口为 `<production-web-url>`；MCP 入口为 `<production-web-url>/mcp`，无认证 MCP POST 请求返回 401。
- 生产保留 7 个正式账号：3 个 admin、4 个 employee，全部启用且 `must_change_password=true`；七个账号使用私有初始密码逐一调用生产认证接口均返回 200。用户名和密码只进入独立加密凭据包。
- 生产浏览器实测桌面 admin 和 390×844 employee 登录后均进入 `/change-password`，控制台错误为 0；`/register` 自动回到 `/login`。
- 生产 `agent_api.pb.js` SHA-256 为 `ccd32069f3d35d8d388719c55cf3bec207ff3d307c731f425358288746ea0dad`；`user_auth_guard.pb.js` SHA-256 为 `7093d50869446d783e7f9a94f3aaa85a5e344c05c317e2e5ac4de136b73afd7a`。
- MCP 暴露 20 个业务工具，不包含账号或系统管理工具。环境文件权限为 `0600`；release 由 root 管理，运行用户只读；`systemd-analyze security` 评分为 `3.2`。
- 3.06 Debug APK 的 versionName 为 `3.06`、versionCode 为 `46`，大小 `7,210,979` bytes，SHA-256 为 `C445196B7574C63CDE4F3E90E14EB6CAD4929EDF4717DE41B7D78C2FF52B87ED`，版本和包信息静态验收通过。APK 作为明确标注的 Android 内测包提供；`adb devices` 当前为空，真机仍待验收。
- 公网 8090 因 3.06 APK 尚未完成真机验收而保留；完成真机全链路验证后再关闭。
- 当前服务器地址、SSH 私钥路径、精确备份路径和交付物路径只记录于私有运维接力文档，不进入公开仓库。
- WorkBuddy 的脱敏 Skill、配置模板和使用指南进入仓库；实际 MCP URL/Token 只进入独立 AES 加密 Agent 接入卡，且不与员工账号密码混发。老板只读模式目前是 Agent 行为约束，不是独立服务端只读 Token。

MCP 高风险操作的 preview 会返回一次性确认码。当前实现允许同一 Agent 连续调用 preview 和 confirm，并未强制独立人工审批。交付流程要求人工检查 preview 内容和确认码后明确授权；Agent 不得自动串联 preview 与 confirm。

当前提交树的敏感信息扫描未发现真实密码、Token、API Key 或私钥。旧 Git 历史仍可见已经删除的演示账号和演示默认密码文案；在禁止 force push 的约束下不重写历史。演示账号不属于 3.06 正式账号，但所有正式用户仍须首次登录立即改密。

## 生产发布检查点

1. 每次发布仍需重新冷备 `pb_data`，确认业务库 `quick_check=ok`。
2. 先比对生产 `_migrations` 与仓库文件，只部署尚未应用且经过审查的迁移；历史分叉环境禁止整目录覆盖。
3. 跑账号、权限、事务、通知和 AI 负面/正面测试。
4. PocketBase、MCP sidecar、Web 与 AI Provider 已发布并验收；3.06 APK 仍需真机覆盖安装复验。
5. 真实员工账号按 username 幂等同步；用户名、角色和初始密码均以私有部署清单为准。
6. 检查 GitHub PR 文件树、敏感信息、提交消息和 CI/本地测试。

## 接力记录格式

每次接手只追加当前事实，不复制旧长篇会话：

```text
时间：
目标：
分支/提交：
修改文件：
验证命令与结果：
生产部署状态：
未完成/回滚点：
```
