# 需求与验收矩阵

状态含义：`已验证` 表示本轮有自动化或隔离数据库证据；`待生产验收` 表示代码完成但尚未在生产全链路确认；`未交付` 表示本版本不宣称完成。

| 需求 | 状态 | 验收证据/边界 |
|---|---|---|
| 单一跨端主导航 | 已验证 | Playwright 覆盖 390、820、900、1440 宽度与三种角色 |
| 角色路由与旧链接兼容 | 已验证 | Vitest + Playwright |
| 项目卡片进入项目详情 | 已验证 | Playwright 项目详情直达与手机无溢出 |
| 项目详情、看板、时间轴互通 | 已验证 | 路由/组件 E2E；生产项目详情直达；生产看板独立手柄跨泳道更新成功并恢复原状态 |
| 手机紧凑密度 | 已验证 | 390x844 项目、资料、时间轴 E2E；仍需真机视觉复核 |
| 姓名字标头像与资料页 | 已验证 | 中文姓名取末两个字的单测 + 资料页 E2E；真实照片优先 |
| 保持登录/会话恢复 | 已验证 | HybridAuthStore 单测与浏览器重开测试 |
| 注册后直接登录 | 已验证 | 公共路由与隔离库账号流程；生产是否开放注册由权限规则决定 |
| 管理员新增/编辑/停用账号 | 已验证 | 隔离数据库人员维护 QA `32/32` + Web 用户管理回归 |
| 停用账号旧 Token 失效 | 已验证 | 密码登录与 auth-refresh 均返回 401 |
| 防止自降权/删除历史账号 | 已验证 | 服务端 Hook 负面测试 |
| 项目/任务最小可见范围 | 已验证 | 外部员工项目列表为 0；项目成员可见 |
| 完成任务与交接原子化 | 已验证 | 成功、重复 409、并发审批 200+409 |
| 卡点/回退/解阻原子化 | 已验证 | 状态与前序任务同步；失败输入不污染数据 |
| 任务永久删除依赖清理 | 已验证 | 自定义事务路由删除并重算项目进度 |
| 项目永久删除 | 未交付 | 使用归档替代，避免跨集合半删除 |
| 通知分页与链接 | 已验证 | 单测/E2E；生产 Realtime SSE 返回 200，Web 通知可无刷新更新；3.06 Android 系统通知仍待真机复验 |
| 通用 LLM Provider | 已验证 | 配置解析单测；给定 Volcengine OpenAI-compatible 接口已做临时上游调用测试，Key 未入库 |
| AI 生产调用 | 已验证 | 管理员页面保存通用 Provider；脱敏读取不返回 Key；服务端代理真实调用返回 200、`choices` 与非空内容 |
| PocketBase systemd 与日志修复 | 已验证 | 服务器已完成 logs.db 隔离、systemd 自动拉起与健康检查 |
| 最新权限/事务 Hook 生产部署 | 已验证 | 基线提交 `c4c298f` 于 2026-08-20 06:45 CST 部署；删除影响预检兼容修复提交 `7bf595f` 于 2026-09-02 部署；生产 Hook SHA-256 与当前交接记录一致，`tasks` 缺少历史 `approved_by` 字段时按实际 schema 跳过 |
| MCP sidecar 生产部署 | 已验证 | `<production-web-url>/mcp`；24 个工具（含受控人员维护）；无认证 MCP POST 返回 401；环境文件 `0600`；release 运行时只读；systemd 安全评分 `3.2` |
| 生产 Web 3.06 | 已验证 | `<production-web-url>` 已发布 3.06；生产 Realtime SSE 返回 200 |
| 生产 Web HTTPS | 已验证 | 根入口和 MCP 均通过 HTTPS 提供服务 |
| Debug APK 构建 | 已验证 | Capacitor sync + Gradle assembleDebug |
| APK 真机完整回归 | 待生产验收 | 3.06 / 46 静态验收通过并作为 Android 内测包提供；`adb devices` 为空，真机仍待验收 |
| 正式账号与强制改密 | 已验证 | 7 个账号逐一生产认证返回 200；桌面 admin 与手机宽度 employee 均进入 `/change-password` |
| Release APK/商店签名 | 未交付 | 缺私有 release keystore 和发布流程 |
| 可发布小程序 | 未交付 | 仅导航模型/API 边界无 DOM 依赖 |

## 质量门禁

- 后端 QA 既有基线 `64/64`、最终差异 `15/15`。
- 前端测试 `204/204`，Playwright `30/30`。
- MCP 测试 `14/14`；人员维护隔离 QA `32/32`，临时账号和数据库已清理。
- TypeScript/Vite 生产构建通过。
- 本轮变更源文件 ESLint 通过；全仓历史 lint 债务不得新增。
- 所有 hooks/migrations 通过 `node --check`。
- 发布前必须再次运行生产副本权限和事务负面测试。
