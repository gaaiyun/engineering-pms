---
description: 测试分层与命名规范（Vitest + Testing Library）
globs: frontend/src/**/*.test.{ts,tsx}
alwaysApply: false
---

# 测试规范

## 工具链

| 工具 | 用途 |
|------|------|
| Vitest | 测试运行器 + 断言库 |
| @testing-library/react | 组件渲染与交互测试 |
| @testing-library/jest-dom | DOM 断言扩展 |
| @testing-library/user-event | 用户交互模拟 |
| happy-dom | 浏览器环境模拟 |

## 命令

```bash
npm test          # 运行全部测试
npm run test:watch # 监听模式
npm run test:coverage # 覆盖率报告
```

## 目录与命名

- 测试文件与源码**同目录**放置
- 命名：`<源文件名>.test.ts` 或 `<源文件名>.test.tsx`
- Setup 文件：`src/test/setup.ts`

## 测试分层

1. **单元测试**：`lib/` 下的纯函数（task-parser, queryKeys, isManagerRole）
2. **组件测试**：UI 组件的渲染与交互（render + screen）
3. **集成测试**：TanStack Query hooks（renderHook + QueryClientProvider + Mock pb）

## Mock 约定

- Mock PocketBase：`vi.mock('../lib/pocketbase')` 然后 mock `pb.collection()` 返回值
- Mock 数据结构必须与 `api.ts` 中的类型定义一致
- 不 mock TanStack Query 本身，使用真实的 QueryClient

## 用例书写

```typescript
describe('模块名', () => {
  it('应该做什么', () => {
    // Arrange - 准备
    // Act - 执行
    // Assert - 断言
  })
})
```

- 只测用户可见行为，不测内部实现
- 每个 it 只测一个行为点
