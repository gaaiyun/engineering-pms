---
description: API、缓存、事务与通知边界
globs: frontend/src/lib/**/*.ts
alwaysApply: false
---

# API 与数据流

- 查询优先复用 `lib/api.ts` hooks 和 `queryKeys`。
- 非幂等 mutation 不自动 retry；成功后 invalidate 直接受影响的 tasks/projects/handoffs/notifications/audit keys。
- 完成、卡点、解阻、交接审批和任务删除必须调用服务端事务路由。
- 可信通知由 Hook/服务端命令创建，浏览器不能直接给其他用户创建通知。
- 普通审计记录只更新 `review_status=read`；业务批准/驳回只在 handoff 流程。
- Realtime 事件只负责刷新缓存，不复制业务副作用。
- employee 查询必须受项目成员/任务负责人规则限制；全局搜索不能绕过集合规则。
- 错误消息使用 `getPocketBaseErrorMessage`，不得输出 token、Key 或上游响应全文。
