---
description: 角色、路由与业务状态机
alwaysApply: false
---

# 角色

| 角色 | 能力 |
|---|---|
| employee | 个人任务、参与项目、完成交接、卡点、通知、资料 |
| manager | 项目/任务/成员管理、审核中心、AI 分析 |
| admin | manager 全部能力，加用户、Provider、导入和系统设置 |

- `/system/*` 只允许 admin；`/review-center` 和任务创建允许 manager/admin。
- `AppShell` 是唯一主导航；手机固定工作台、任务、项目、通知、我的。
- 任务完成必须提交 handoff；卡点必须填写原因；交接只有 pending -> approved/rejected 一次状态变化。
- 看板拖到完成/卡点只能打开详情表单，不能直接 PATCH 状态。
- 项目使用归档；任务永久删除走服务端事务。
- 停用用户不能登录/refresh；管理员不能自降权；有历史引用的账号只能停用。
- 测试账号由隔离数据库创建，不把账号密码写入规则或文档。
