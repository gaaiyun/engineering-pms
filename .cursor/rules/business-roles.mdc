---
description: 角色权限与业务场景（经理/员工边界、页面访问、操作限制）
alwaysApply: false
---

# 角色权限与业务场景

## 角色矩阵

| 角色 | 可见范围 | 典型能力 |
|------|---------|---------|
| admin/manager | 全部项目、管理后台 | 创建/编辑/删除项目和任务，管理成员，归档，审批，批量编辑 |
| employee | 仅参与的项目 | 查看项目与任务、上报卡点；不可编辑任务/项目 |

## 页面访问控制

| 页面 | 路由保护 | 允许角色 |
|------|---------|---------|
| AdminDashboard | AdminRoute | admin, manager |
| ManagerDashboard | ManagerRoute | admin, manager |
| ReviewCenter | ManagerRoute | admin, manager |
| TaskCreate | ManagerRoute | admin, manager |
| 其他业务页面 | PrivateRoute | 所有已登录用户 |

## 权限判断方式

- 路由层：`App.tsx` 中的 `AdminRoute` / `ManagerRoute` 组件
- 业务层：`api.ts` 中的 `isManager()` 函数
- 直接判断：`pb.authStore.model?.role?.toLowerCase()`

## 员工限制（必须遵守）

- 员工不出现「管理」Tab
- 员工在项目详情/任务详情中无「编辑任务」「分配人员」「删除任务」按钮
- 员工不能拖拽看板卡片（KanbanBoard 中 sensors 置空）
- 员工只能上报卡点（填写原因），不能编辑其他字段

## 卡点流程

- 员工上报卡点：只需填写原因（reason_type + reason_detail）
- 取消卡点时可选择变为「已完成」或「进行中」
- 卡点状态在看板中单独一列显示

## 测试账号

| 角色 | 账号 | 密码 |
|------|------|------|
| 管理员 | zhao_boss | 12345678 |
| 经理 | zhang_manager | 12345678 |
| 员工 | li_audit | 12345678 |
| 员工 | chen_doc | 12345678 |
