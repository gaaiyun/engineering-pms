---
description: TypeScript、React 与跨端 UI
globs: frontend/src/**/*.{ts,tsx}
alwaysApply: false
---

# TypeScript / React

- 禁止新增 `any`；外部输入先视为 `unknown` 并验证。
- 服务端数据用 TanStack Query，局部 UI 用 React state，跨组件草稿可用 Zustand。
- 页面直接调用 PocketBase 仅限尚无 hook 的单记录操作，并需错误、loading 和缓存处理。
- antd-mobile Selector 返回数组，提交前显式解包。
- 不在 effect 中制造无必要 setState 循环；订阅 cleanup 只取消本组件注册的目标。
- 手机字号和间距以信息密度为先，同时保持约 44px 可点击区域；底栏不能遮挡页面操作。
- 桌面不显示手机底栏，手机/App 不显示侧栏；页面内部不再创建第二套主导航。
- 项目详情、看板、时间轴和任务详情必须互相可达且刷新后可直达。
- 组件与测试同目录；用户可见行为用 Testing Library/Playwright 验证。
